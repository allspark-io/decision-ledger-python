"""Framework-specific integrations. Each submodule is a thin, optional
adapter — not imported by the core package, so installing
allspark-decision-ledger never pulls in a framework dependency you don't use.
"""
